apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-statefulset
spec:
  selector:
    matchLabels:
      app: postgres
  replicas: {{ .Values.postgres.replicas }}
  serviceName: postgres
  template:
    metadata:
      labels:
        app: postgres
      annotations:
        helm.sh/revision: "{{ .Release.Revision }}"
    spec:
      securityContext:
        fsGroup: 999
      volumes:
        - name: configs
          configMap:
            name: postgres-configs
            defaultMode: 0400
        - name: tls
          secret:
            secretName: tls-secret
            defaultMode: 0400
        - name: data-volume
          persistentVolumeClaim:
            claimName: postgres-pvc
      containers:
        - name: postgres
          image: {{ .Values.postgres.image }}
          args: [ -c, config_file={{ .Values.postgres.configs.main.path }} ]
          env: {{- include "env" (dict "_" $ "_envs" .Values.postgres.envs) | indent 12 }}
          ports: {{- include "ports" .Values.postgres.ports | indent 12 }}
          volumeMounts: {{- include "configs" .Values.postgres.configs | indent 12 }}
            - name: tls
              mountPath: /tls/
            - name: data-volume
              mountPath: /var/lib/postgresql/data/
          resources: {{- toYaml .Values.postgres.resources | nindent 12 }}
