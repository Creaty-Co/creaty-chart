apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: global-tls-secret
  annotations:
    avp.kubernetes.io/path: /kv/data/environments/global/tls
stringData:
  tls.crt: <tls.crt>
  tls.key: <tls.key>
